你在帮我把 **{{NAME}}（{{SYMBOL}}）** 最近的资讯捋成一条「这只股票最近在发生什么」的线，讲给一个没空逐条读新闻的朋友听。下面每条带编号 `[n]`、日期、来源、标题。

资讯条目：
{{ITEMS}}

请**只输出一个 JSON 对象**（不要任何额外文字、不要代码围栏）：

```
{
  "summary": "2–4 句大白话：这只股票现在的主线是什么、为什么值得关注、有什么争议或风险。像聊天一样讲清楚，别用'持续推进''战略布局''备受关注'这类空话套话。",
  "timeline": [
    {
      "date": "YYYY-MM-DD",
      "title": "一句话说清这件事——发生了什么、具体到数字/客户/产品，别只照搬英文标题",
      "importance": "critical|high|med|low",
      "refs": [1, 4]
    }
  ]
}
```

硬规则（违反即失败）：
- **只用上面给出的条目**，不引入任何未出现的事实、数字、传闻或先验知识。
- `refs` 编号**必须真实存在**于上面列表，每个事件至少 1 条；指向同一事件的多条**合并**成一个 timeline 项。
- `title` 用**中文大白话**，写清「谁干了什么、结果如何」（拿下 X 大单 / 被纳入 X 指数 / 财报营收 X 超预期 / 被 X 做空 / X 高管离职），能带数字就带。
- 时间线按日期**从新到旧**，最多 ~12 个最有信息量的事件，琐碎重复的略去。
- `importance`：能改变投资逻辑的（重大合同、并购、财报、监管、做空）→ critical/high；常规跟踪 → med；边角 → low。
- 资讯稀少或多为泛泛而谈时，`summary` 如实说「最近消息不多」，timeline 可短，**不要凑数编造**。
