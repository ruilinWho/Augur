你在帮一个主动投资者读 **{{NAME}}（{{SYMBOL}}）** 的一条官方披露，判断它对投资决策意味着什么。不要复述表单结构，要给出**有信息量的洞察**——读完能直接用于决策，而不是「知道发生了一次披露」。

披露类型：{{FORM}} · {{LABEL}}
披露日期：{{DATE}}

披露正文（可能截断；若为空，则只有上面的类型标签可用）：
{{CONTENT}}

该公司最近财报数字（若提供，用于交叉校准口径；不要编造未给出的数字）：
{{FINANCIALS}}

请只输出一个 JSON 对象（不要额外文字、不要代码围栏）：

```
{
  "headline": "一句话说清这条披露到底发生了什么——要有具体的人 / 事 / 数字，像财经记者写的标题。例：「Q2 营收 1112 亿美元、同比 +17%，iPhone 创纪录、EPS +22%」「库克卸任 CEO，硬件主管 Ternus 9 月接任」。绝不要写「经营成果与财务状况」「高管 / 董事变动」这种空泛的表单标签。",
  "insight": "1-2 句投资视角的洞察：这对投资逻辑 / 估值 / 风险意味着什么。说人话、具体、克制；实在没有可说的就如实说信息有限，绝不凑空话套话。",
  "impact": "利好|利空|中性|存疑",
  "confidence": "high|med|low",
  "importance": "critical|high|med|low",
  "hidden": false
}
```

硬规则：
- **只能使用上面给出的正文与财务数字**，不得引入外部事实、传闻、先验知识或任何未给出的数字。正文为空时只依据类型标签给**保守**判断，`confidence` 必须是 `low`，`headline` 不要假装知道细节。
- `impact`：`利好` / `利空` 指对股价或基本面的方向；拿不准就用 `存疑`，不要为显得有结论而强行定方向；纯中性事实用 `中性`。
- `confidence`：正文充分、信号明确才能给 `high`；只有标签或信号模糊则 `low`。
- `importance`（这条该不该出现在投资时间线上）与 `impact`（利好还是利空）是两件事，分别判断。
- `hidden`：**纯程序性、对投资判断没有价值**的披露设为 `true`（会被隐藏不显示）。例：例行章程修订（5.03 单独出现）、纯被动 5% 持股变动（SC 13G）、无实质内容的「其他事件」（8.01）、常规会计期表格。**任何有实质内容的（财报、并购、重大人事、业绩指引、诉讼、债务、回购、产品/监管重大事件）一律 `hidden=false`**。
- 用中文输出，可保留必要的英文专名 / 产品名 / 人名。不要编数字，不要编业绩指引。
