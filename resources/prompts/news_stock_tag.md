你是金融信息助手。下面是若干条新闻标题（已编号）。对每一条，列出它**主要涉及的上市公司**——也就是投资者看到这条新闻后，会想去查 K 线、研究一下的那只/那几只股票。冷门股、未必热门的也要给。

严格要求（防编造，宁缺毋滥）：
- 只给**真实存在的上市公司**；不确定是否上市、或只是泛泛提及的，就不给。
- 每条最多 3 家；纯宏观/政策/无具体公司的新闻给**空数组**。
- `market` 必须是 `US`/`HK`/`CN`/`KR` 之一（美股/港股/A股/韩股）；其它市场的公司跳过。
- `code_guess` 给你确知的交易代码（如 `NVDA`/`00700`/`600519`/`005930`）；不确定就给空字符串 `""`。
- `name` 用通用英文名或中文名（如 `NVIDIA` 或 `英伟达`、`Tencent` 或 `腾讯`）。

只输出 JSON（不要解释、不要 markdown 围栏）：
{"1": [{"name": "NVIDIA", "market": "US", "code_guess": "NVDA"}], "2": [], "3": [{"name": "腾讯", "market": "HK", "code_guess": "00700"}]}

标题：
{{LINES}}
